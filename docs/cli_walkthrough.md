# Command-line interface and executable walkthrough

Install `protein-fasta[cli]`. The wheel declares one console-script entry point:

```text
protein-fasta = protein_fasta.cli:main
```

Every operation is a Cyclopts subcommand of `protein-fasta`, not a separate installed script.
Parameters are generated by Cyclopts, and progress/reporting goes through Loguru. Run
`protein-fasta --help` or `protein-fasta <command> --help` for the generated option reference.

## Command map

| Area | Command syntax | Result |
| --- | --- | --- |
| FASTA table | `basic FASTA-PATH TABLE-PATH` | Stable `id`, `description`, and optional `sequence` table |
| FASTA table | `table [OPTIONS] FASTA-PATH TABLE-PATH` | Row-wise built-in enrichment, or whole-file enrichment with `--strict` |
| FASTA table | `configured --rules PATH... --classifiers PATH [OPTIONS] FASTA-PATH TABLE-PATH` | Enrichment using explicit parser and classifier documents |
| FASTA table | `formats FASTA-PATH TABLE-PATH` | One recognition-diagnostic row per built-in format |
| FASTA evidence | `diagnostics FASTA-PATH` | Record, residue, length, namespace, normalization, and classification summary |
| FASTA evidence | `checksum FASTA-PATH` | Exact-file MD5 and normalized ID/sequence-content fingerprint |
| Peptide table | `digest [OPTIONS] FASTA-PATH TABLE-PATH` | Theoretical peptides and missed-cleavage counts |
| UniProt | `uniprot-catalog --output DIR` or `--request PATH` | Immutable local proteome-catalog snapshot |
| UniProt | `uniprot-proteomes [OPTIONS] CATALOG-PATH TABLE-PATH` | Local catalog filter; no provider request |
| UniProt | `uniprot-download PROTEOME MODE` or `--request PATH` | One acquired source FASTA plus reproducible request/evidence; no build or decoys |
| Protein input | `prepare FASTA OUTPUT --id ID` or `--request PATH` | Canonical protein-input Parquet from ordered FASTA sources |
| Protein input | `derive-input INVENTORY OUTPUT --id ID` or `--request PATH` | Clean protein-input Parquet derived from existing inventories |
| Database | `build PROTEIN-INPUT --output DIR ...` or `--request PATH` | Decoy-free biological FASTA and protein inventory |
| Database | `decoy BIOLOGICAL-INVENTORY --output FASTA --method METHOD` or `--request PATH` | Search FASTA and search inventory for one required strategy |
| Peptides | `peptides INVENTORY --output DIR` or `--request PATH` | Peptide inventory, mapping, and unique-peptide FASTA |
| Peptides | `pepcompare PEPTIDES-A PEPTIDES-B --output PATH` or `--request PATH` | Exact comparison of two canonical peptide inventories |
| Decoy analysis | `decoy-report BIOLOGICAL-INVENTORY --output PATH --method METHOD` or `--request PATH` | Peptide-level comparison of requested decoy methods |
| Registry | `index [OPTIONS] FASTA-DIR REGISTRY-PATH` | Build or update a SQLite or DuckDB registry from FASTA |
| Registry | `index-inventory [OPTIONS] INVENTORY-PATH REGISTRY-PATH` | Index one canonical biological or search inventory |
| Registry | `candidate INVENTORY REGISTRY --output PATH` or `--request PATH` | Compare a candidate inventory without registering it |
| Registry | `registry REGISTRY-PATH TABLE-PATH` | Export database summaries and fingerprints |
| Registry | `compare [OPTIONS] REGISTRY-PATH DATABASE-ID TABLE-PATH` | Compare one indexed database with every detailed database |
| Registry | `pairs [OPTIONS] REGISTRY-PATH OUTPUT` | Export long pair metrics and optional Jaccard matrices |
| Registry | `cluster [OPTIONS] REGISTRY-PATH TABLE-PATH` | Export deterministic average-linkage merge rows |

## Command and artifact rules

- CSV, TSV, XLSX, and Parquet table formats are selected by output suffix.
- `--no-sequence` applies to protein-row exports. It is not a generic output switch.
- Request-document paths are resolved relative to the document containing them.
- A direct workflow command writes a validated authored `*.request.json` before computation.
  `--request PATH` replays it; `--output` may redirect replayed products without changing policy.
- Authored requests are never overwritten. Use `--save PATH` to choose their first-run location.
- `prepare` writes the canonical protein-input Parquet. `build` consumes it and produces only a
  decoy-free biological FASTA and protein inventory.
- `decoy` is a subsequent operation over the biological inventory. Its request must name exactly
  one strategy; callers that need only the biological database do not invoke it.
- `candidate` is read-only. `index-inventory` is the preferred post-approval registry handoff.
- SQLite and DuckDB are selected by registry suffix and, when creating a registry, matching
  registry configuration.

## UniProt catalog and acquisition

Catalog synchronization and FASTA download contact UniProt and therefore are not run during a
documentation build. Filtering an existing catalog is local, but it requires the snapshot created
by `uniprot-catalog`.

Start a reference-proteome catalog directly:

```bash
protein-fasta uniprot-catalog --output catalog
```

Before contacting UniProt, this writes `catalog/uniprot-catalog.request.json`:

```json
{
  "output_dir": ".",
  "schema_version": "0.1",
  "selection": {
    "type": "reference"
  },
  "timeout_seconds": 120.0
}
```

- `schema_version` selects the validated catalog-request contract.
- `output_dir` is relative to the request file, so `.` means its `catalog/` directory.
- `selection.type` requests the UniProt reference-proteome catalogue.
- `timeout_seconds` records the provider timeout used by the first run.

Replay the saved selection, optionally redirecting the snapshot:

```bash
protein-fasta uniprot-catalog \
  --request catalog/uniprot-catalog.request.json \
  --output replayed-catalog
```

The direct download form takes a proteome identifier and one acquisition mode:

- `reviewed` — reviewed Swiss-Prot entries;
- `canonical` — the complete proteome's reviewed and unreviewed canonical entries; or
- `opg` — one canonical protein sequence per gene from UniProt GeneCentric.

For example:

```bash
protein-fasta uniprot-download UP000000589 opg --output mouse-opg.fasta
head -n 3 mouse-opg.fasta
```

Before acquisition, this writes `mouse-opg.fasta.request.json`:

```json
{
  "acquisition": {
    "type": "one_seq_per_gene"
  },
  "output_fasta": "mouse-opg.fasta",
  "schema_version": "0.1",
  "selection": {
    "proteome_id": "UP000000589",
    "type": "proteome_id"
  },
  "timeout_seconds": 120.0
}
```

- `selection.type` says the source is an explicit proteome rather than a taxonomy search.
- `selection.proteome_id` records the chosen proteome.
- `acquisition.type` records the canonical-per-gene behavior selected by `opg`.
- `output_fasta` is the FASTA path relative to the request.
- `timeout_seconds` and `schema_version` have the same meanings as in the catalog request.

Replay it explicitly:

```bash
protein-fasta uniprot-download \
  --request mouse-opg.fasta.request.json \
  --output replayed-mouse-opg.fasta
```

After catalog synchronization, local filtering uses ordinary arguments and no JSON:

```bash
protein-fasta uniprot-proteomes uniprot-proteomes.parquet proteomes.csv \
  --query "Homo sapiens"
head -n 3 proteomes.csv
```

`uniprot-download` acquires exactly one source FASTA. It does not prepare sources, build a
database, add decoys, install, or index. Result evidence records the selected proteome and
resolution route, provider query, observed release headers, observed and provider-reported counts,
checksum, byte size, and warnings.

## Executable offline walkthrough

The remainder of this page executes 21 of the 24 subcommands against two deterministic 50-entry
UniProtKB FASTA files and one 50-entry mixed UniProtKB/RefSeq file. The console blocks are not
pasted examples: Zensical runs them during `make docs`, captures their output, and fails the build
if any command exits non-zero.

`$WORK` is a fresh temporary directory used only for this documentation build.

## Prepare the isolated example

{{ cli('cp -R docs/examples/cli/. "$WORK/"') }}

{{ cli('mkdir "$WORK/registry-input"') }}

{{ cli('cp "$WORK/alpha.fasta" "$WORK/beta.fasta" "$WORK/registry-input/"') }}

## Export the basic table

`basic` always emits the stable base schema. Here the sequence is deliberately excluded.

{{ cli('protein-fasta basic "$WORK/alpha.fasta" "$WORK/basic.csv" --no-sequence') }}

{{ cli('head -n 3 "$WORK/basic.csv"') }}

## Export the automatically enriched table

`table` recognizes this homogeneous UniProtKB input and adds the configured header fields.

{{ cli('protein-fasta table "$WORK/alpha.fasta" "$WORK/proteins.csv" --no-sequence') }}

{{ cli('head -n 3 "$WORK/proteins.csv"') }}

## Use explicit parser documents

`configured` applies explicitly selected database-format and entry-classifier JSON documents.

{% set configured_command %}
protein-fasta configured "$WORK/alpha.fasta" "$WORK/configured.csv" \
  --rules src/protein_fasta/documents/frame_formats/uniprotkb/rules.json \
  --rules src/protein_fasta/documents/frame_formats/refseq/rules.json \
  --classifiers src/protein_fasta/documents/entry_classifiers/rules.json \
  --no-sequence
{% endset %}
{{ cli(configured_command) }}

{{ cli('head -n 3 "$WORK/configured.csv"') }}

## Diagnose a mixed database

The mixed database contains 25 UniProtKB records and 25 RefSeq records. Format detection reports
both partial matches.

{{ cli('protein-fasta formats "$WORK/mixed.fasta" "$WORK/formats.csv"') }}

{{ cli('head -n 3 "$WORK/formats.csv"') }}

## Automatically parse a mixed database

`table` applies the sole matching built-in parser to each row. The first preview row is UniProtKB;
the second is RefSeq, and both are enriched in one table.

{{ cli('protein-fasta table "$WORK/mixed.fasta" "$WORK/mixed-auto.csv"') }}

{{ cli('head -n 3 "$WORK/mixed-auto.csv"') }}

## Apply only the UniProtKB parser to a mixed database

With only the UniProtKB rule configured, matching rows are enriched. RefSeq rows remain present
with `id`, `description`, and `sequence`; the UniProtKB fields are null.

{% set mixed_uniprot_command %}
protein-fasta configured "$WORK/mixed.fasta" "$WORK/mixed-uniprot.csv" \
  --rules src/protein_fasta/documents/frame_formats/uniprotkb/rules.json \
  --classifiers src/protein_fasta/documents/entry_classifiers/rules.json
{% endset %}
{{ cli(mixed_uniprot_command) }}

{{ cli('head -n 3 "$WORK/mixed-uniprot.csv"') }}

## Require one format for the complete database

`--strict` declines partial enrichment. Because the database is mixed, the result has exactly the
three base columns.

{% set strict_mixed_command %}
protein-fasta configured "$WORK/mixed.fasta" "$WORK/mixed-strict.csv" \
  --rules src/protein_fasta/documents/frame_formats/uniprotkb/rules.json \
  --classifiers src/protein_fasta/documents/entry_classifiers/rules.json \
  --strict
{% endset %}
{{ cli(strict_mixed_command) }}

{{ cli('head -n 3 "$WORK/mixed-strict.csv"') }}

## Parse FGCZ `Cont_` and decorated identifiers

The application-specific classifier marks `sp|Cont_...` as a contaminant without changing the
identifier. A leading `REV_` is removed only from the temporary parsing identifier, so the second
row is both decoy and contaminant. Both rows still receive all UniProtKB metadata.

{% set decorated_command %}
protein-fasta configured "$WORK/decorated.fasta" "$WORK/decorated.csv" \
  --rules src/protein_fasta/documents/frame_formats/uniprotkb/rules.json \
  --classifiers "$WORK/fgcz-classifiers.json" \
  --no-sequence
{% endset %}
{{ cli(decorated_command) }}

{{ cli('head -n 3 "$WORK/decorated.csv"') }}

## Summarize record diagnostics

The report includes record and residue counts, the complete length summary, amino-acid counts and
percentages, identifier namespaces, overlapping classifications, normalization changes, and illegal
sequence characters.

{% set diagnostics_command %}
protein-fasta diagnostics "$WORK/alpha.fasta" \
  --rules src/protein_fasta/documents/diagnostics/rules.json \
  --classifiers src/protein_fasta/documents/entry_classifiers/rules.json
{% endset %}
{{ cli(diagnostics_command) }}

## Digest proteins

The default digestion uses packaged trypsin rules. An explicit reusable digestion policy sits
beside the command that consumes it:

{{ cli('sed -n \'1,60p\' "$WORK/digestion.json"') }}

- `enzyme` selects the separately versioned packaged cleavage expression.
- `min_length` and `max_length` are inclusive peptide-length bounds.
- `missed_cleavages` is the maximum number retained in one theoretical peptide.

{% set digest_command %}
protein-fasta digest "$WORK/alpha.fasta" "$WORK/peptides.csv" \
  --config "$WORK/digestion.json" \
  --rules src/protein_fasta/documents/enzymes/trypsin/rules.json
{% endset %}
{{ cli(digest_command) }}

{{ cli('head -n 3 "$WORK/peptides.csv"') }}

## Compute checksums and fingerprints

`checksum` reports exact-file `md5-file-v1` and normalized ID/sequence-content
`blake2b-128-id-sequence-pairs-v1`. The labels are part of the result and prevent algorithm
ambiguity.

{{ cli('protein-fasta checksum "$WORK/mixed.fasta"') }}

Per-row hashes are optional columns on `table`:

{% set checksums_command %}
protein-fasta table "$WORK/mixed.fasta" "$WORK/checksums.csv" \
  --strict \
  --checksums \
  --no-sequence
{% endset %}
{{ cli(checksums_command) }}

{{ cli('head -n 3 "$WORK/checksums.csv"') }}

## Build a database

Source preparation and biological assembly are separate commands. The first writes the canonical
protein-input Parquet. The second consumes that Parquet plus the biological build parameters and
writes a decoy-free FASTA and protein inventory. The result records typed artifacts, package
version, checksums, counts, complete length/amino-acid summaries, normalization, and optional
entrapment evidence.

Start source preparation with ordinary CLI arguments:

{% set prepare_command %}
protein-fasta prepare \
  "$WORK/alpha.fasta" \
  "$WORK/protein-input.parquet" \
  --id alpha-uniprot
{% endset %}
{{ cli(prepare_command) }}

The direct command wrote this authored request before reading the FASTA:

{{ cli('sed -n \'1,80p\' "$WORK/protein-input.parquet.request.json"') }}

- `schema_version` selects the protein-input request contract.
- `sources` is the ordered input list. This direct form creates one `target` source.
- `source_id` is the stable provenance label supplied by `--id`.
- `path` is recorded relative to the request file.
- `output_parquet` names the canonical prepared protein table, also relative to the request.

The saved request can be replayed while redirecting only its output:

{% set prepare_replay %}
protein-fasta prepare \
  --request "$WORK/protein-input.parquet.request.json" \
  --output "$WORK/replay/protein-input.parquet"
{% endset %}
{{ cli(prepare_replay) }}

Now start the biological build directly:

{% set build_command %}
protein-fasta build "$WORK/protein-input.parquet" \
  --output "$WORK/out" \
  --project 1 \
  --dbn 1 \
  --description walkthrough \
  --date 2026-08-27
{% endset %}
{{ cli(build_command) }}

{{ cli('sed -n \'1,100p\' "$WORK/out/build.request.json"') }}

- `schema_version` selects the biological-build request contract.
- `output_dir` is `.` because the request lives inside the selected output directory.
- `date` is the reproducible build date used in the FASTA name and metadata.
- `name_fields` supplies `project`, `dbn`, and `description` to the packaged naming template.
- `annotation` is the optional free-text database annotation; the direct form records it empty.

This authored request contains intent. The sibling `*.effective.json` records the fully resolved
naming and metadata policy, while `*.result.json` records observed artifacts, checksums, counts,
summaries, and warnings.

{% set build_replay %}
protein-fasta build "$WORK/protein-input.parquet" \
  --request "$WORK/out/build.request.json" \
  --output "$WORK/replay-build"
{% endset %}
{{ cli(build_replay) }}

{{ cli('head -n 3 "$WORK/out/p1_db1_walkthrough_20260827.fasta"') }}

## Generate the search database

Decoy generation is a subsequent operation over the biological inventory. It does not repeat
source preparation or biological assembly.

{% set decoy_command %}
protein-fasta decoy \
  "$WORK/out/p1_db1_walkthrough_20260827.fasta.protein-inventory.parquet" \
  --output "$WORK/out/p1_db1_walkthrough_d_20260827.fasta" \
  --method reverse
{% endset %}
{{ cli(decoy_command) }}

{{ cli('sed -n \'1,80p\' "$WORK/out/p1_db1_walkthrough_d_20260827.fasta.request.json"') }}

- `schema_version` selects the decoy request contract.
- `output_fasta` is the search FASTA path relative to the request.
- `decoy_prefix` records the generated identifier decoration.
- `strategy.type` records the selected reverse algorithm. Direct mode also offers shuffle;
  DecoyPYrat and custom strategy details are advanced request-JSON variants.

{% set decoy_replay %}
protein-fasta decoy \
  "$WORK/out/p1_db1_walkthrough_20260827.fasta.protein-inventory.parquet" \
  --request "$WORK/out/p1_db1_walkthrough_d_20260827.fasta.request.json" \
  --output "$WORK/replay-search.fasta"
{% endset %}
{{ cli(decoy_replay) }}

{{ cli('head -n 3 "$WORK/out/p1_db1_walkthrough_d_20260827.fasta"') }}

## Index the example collection with SQLite

`index` can incrementally update an existing registry. `--full` rebuilds it atomically,
`--force` revisits unchanged files, `--prune` removes missing sources, and `--recursive` includes
supported FASTA files in subdirectories.

{{ cli('protein-fasta index "$WORK/registry-input" "$WORK/registry.sqlite3"') }}

## Index the example collection with DuckDB

DuckDB selection is reusable registry policy, not a generated per-run request:

{{ cli('sed -n \'1,60p\' "$WORK/registry-duckdb.json"') }}

- `schema_version` selects the registry-policy contract.
- `registry.backend` selects DuckDB when creating the registry and must agree with `.duckdb`.

The same policy file is supplied to indexing, canonical-inventory indexing, and candidate review:

{% set duckdb_index_command %}
protein-fasta index "$WORK/registry-input" "$WORK/registry.duckdb" \
  --config "$WORK/registry-duckdb.json"
{% endset %}
{{ cli(duckdb_index_command) }}

## Index a canonical inventory directly

The preferred handoff indexes the accepted protein or search inventory without reparsing FASTA.

{% set inventory_index_command %}
protein-fasta index-inventory \
  "$WORK/out/p1_db1_walkthrough_d_20260827.fasta.search-inventory.parquet" \
  "$WORK/inventory-registry.duckdb" \
  --config "$WORK/registry-duckdb.json"
{% endset %}
{{ cli(inventory_index_command) }}

## Review a candidate without registering it

{% set candidate_command %}
protein-fasta candidate \
  "$WORK/out/p1_db1_walkthrough_d_20260827.fasta.search-inventory.parquet" \
  "$WORK/registry.duckdb" \
  --output "$WORK/out/candidate-comparisons.parquet" \
  --limit 10 \
  --config "$WORK/registry-duckdb.json"
{% endset %}
{{ cli(candidate_command) }}

{{ cli('sed -n \'1,80p\' "$WORK/out/candidate-comparisons.parquet.request.json"') }}

- `schema_version` selects the candidate-review request contract.
- `output_parquet` names the comparison table relative to the request.
- `overlap_threshold` controls containment relationship labels.
- `clustering_metric` selects target-ID or target-sequence neighbourhood distances.
- `neighbour_limit` records the direct `--limit` value.

{% set candidate_replay %}
protein-fasta candidate \
  "$WORK/out/p1_db1_walkthrough_d_20260827.fasta.search-inventory.parquet" \
  "$WORK/registry.duckdb" \
  --request "$WORK/out/candidate-comparisons.parquet.request.json" \
  --output "$WORK/out/replayed-candidate.parquet" \
  --config "$WORK/registry-duckdb.json"
{% endset %}
{{ cli(candidate_replay) }}

## Derive clean source rows from a search database

The derived-input operation retains target and contaminant proteins and explicitly counts prior
sentinel, section-marker, entrapment, and decoy rows that it excludes.

{% set derive_command %}
protein-fasta derive-input \
  "$WORK/out/p1_db1_walkthrough_d_20260827.fasta.search-inventory.parquet" \
  "$WORK/out/derived-protein-input.parquet" \
  --id walkthrough-search-database
{% endset %}
{{ cli(derive_command) }}

{{ cli('sed -n \'1,80p\' "$WORK/out/derived-protein-input.parquet.request.json"') }}

- `schema_version` selects the derived-input request contract.
- `source_inventory` identifies the existing biological or search inventory relative to the
  request.
- `source_id` is the stable provenance label supplied by `--id`.
- `output_parquet` names the filtered protein-input table relative to the request.

{% set derive_replay %}
protein-fasta derive-input \
  --request "$WORK/out/derived-protein-input.parquet.request.json" \
  --output "$WORK/out/replayed-derived-input.parquet"
{% endset %}
{{ cli(derive_replay) }}

## Build canonical peptide artifacts

`peptides` writes canonical peptide and protein-mapping Parquet plus a unique-peptide FASTA.
Memory, SQLite, and DuckDB executors return the same result contract.

{% set peptide_command %}
protein-fasta peptides \
  "$WORK/out/p1_db1_walkthrough_d_20260827.fasta.search-inventory.parquet" \
  --output "$WORK/out" \
  --enzyme trypsin \
  --minimum 7 \
  --maximum 50 \
  --missed 2 \
  --partition 25
{% endset %}
{{ cli(peptide_command) }}

{{ cli('sed -n \'1,120p\' "$WORK/out/peptides.request.json"') }}

- `schema_version` selects the peptide-build request contract.
- `peptides_parquet`, `mapping_parquet`, and `peptide_fasta` name the three canonical products
  relative to the request.
- `digestion` records the enzyme, inclusive length limits, and maximum missed cleavages.
- `execution.type` selects the in-memory executor; `workers` and `partition_size` record its
  deterministic execution settings. SQLite and DuckDB execution are advanced JSON variants.

{% set peptide_replay %}
protein-fasta peptides \
  "$WORK/out/p1_db1_walkthrough_d_20260827.fasta.search-inventory.parquet" \
  --request "$WORK/out/peptides.request.json" \
  --output "$WORK/replayed-peptides"
{% endset %}
{{ cli(peptide_replay) }}

{{ cli('head -n 3 "$WORK/out/peptides.fasta"') }}

## Compare peptide inventories

{% set peptide_comparison_command %}
protein-fasta pepcompare \
  "$WORK/out/peptides.parquet" \
  "$WORK/out/peptides.parquet" \
  --output "$WORK/out/peptide-comparison.parquet"
{% endset %}
{{ cli(peptide_comparison_command) }}

{{ cli('sed -n \'1,60p\' "$WORK/out/peptide-comparison.parquet.request.json"') }}

- `schema_version` selects the peptide-comparison request contract.
- `output_parquet` names the exact population-overlap table relative to the request.

{% set peptide_comparison_replay %}
protein-fasta pepcompare \
  "$WORK/out/peptides.parquet" \
  "$WORK/out/peptides.parquet" \
  --request "$WORK/out/peptide-comparison.parquet.request.json" \
  --output "$WORK/out/replayed-peptide-comparison.parquet"
{% endset %}
{{ cli(peptide_comparison_replay) }}

## Compare decoy methods

{% set decoy_report_command %}
protein-fasta decoy-report \
  "$WORK/out/p1_db1_walkthrough_20260827.fasta.protein-inventory.parquet" \
  --output "$WORK/out/decoy-methods.parquet" \
  --method reverse \
  --enzyme trypsin \
  --minimum 7 \
  --maximum 50 \
  --missed 2
{% endset %}
{{ cli(decoy_report_command) }}

{{ cli('sed -n \'1,120p\' "$WORK/out/decoy-methods.parquet.request.json"') }}

- `schema_version` selects the decoy-report request contract.
- `output_parquet` names the comparison table relative to the request.
- `decoy_prefix` records the decoration shared by the compared methods.
- `digestion` records the enzyme and peptide limits used to compare target and decoy populations.
- `strategies` is the ordered, non-repeating set of methods; reverse carries no ignored seed.

{% set decoy_report_replay %}
protein-fasta decoy-report \
  "$WORK/out/p1_db1_walkthrough_20260827.fasta.protein-inventory.parquet" \
  --request "$WORK/out/decoy-methods.parquet.request.json" \
  --output "$WORK/out/replayed-decoy-methods.parquet"
{% endset %}
{{ cli(decoy_report_replay) }}

## Export the SQLite registry summary

{{ cli('protein-fasta registry "$WORK/registry.sqlite3" "$WORK/registry-sqlite.csv"') }}

{{ cli('head -n 3 "$WORK/registry-sqlite.csv"') }}

## Export the DuckDB registry summary

{{ cli('protein-fasta registry "$WORK/registry.duckdb" "$WORK/registry-duckdb.csv"') }}

{{ cli('head -n 3 "$WORK/registry-duckdb.csv"') }}

## Compare one database

Database identifiers are stable for this path-sorted two-file fixture, so database `1` is selected.
The export contains directional coverage, containment, Jaccard, exact-pair, changed-ID, and
relationship evidence.

{{ cli('protein-fasta compare "$WORK/registry.duckdb" 1 "$WORK/comparison.csv"') }}

{{ cli('head -n 3 "$WORK/comparison.csv"') }}

## Export materialized pair metrics

`pairs` writes long target/contaminant pair metrics and can additionally write symmetric target-ID
and target-sequence matrices.

{% set pairs_command %}
protein-fasta pairs "$WORK/registry.duckdb" "$WORK/pairs.tsv" \
  --ids "$WORK/id-matrix.tsv" \
  --sequences "$WORK/sequence-matrix.tsv"
{% endset %}
{{ cli(pairs_command) }}

{{ cli('head -n 3 "$WORK/pairs.tsv"') }}

## Cluster registered databases

`cluster` performs deterministic average-linkage clustering and logs the leaf order. Use
`--metric target-sequences` to cluster on target sequence Jaccard instead of the default target-ID
Jaccard.

{{ cli('protein-fasta cluster "$WORK/registry.duckdb" "$WORK/clusters.csv"') }}

{{ cli('head -n 3 "$WORK/clusters.csv"') }}
